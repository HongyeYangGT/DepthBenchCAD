# Open items

The benchmark release is complete for task execution and paper-level analysis. Optional future extensions are: an exhaustive all-template real-CadQuery reference audit report, a larger adversarial mutation kill-rate study, and a server-side hidden-test leaderboard. These extensions are not required to use or reproduce the released benchmark.
